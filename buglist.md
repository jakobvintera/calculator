Known bugs (to be fixed):

- if a number with operator is displayed (at which point it has logged the first number and operator) and then the prefix button is pressed, it doesnt log the changed first number, it only changes the display
- if entering a number with zeros after the comma like 42.000000, entering an operator like +, it should clean it up to 42+

